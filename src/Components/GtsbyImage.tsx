// import React, { FunctionComponent } from "react";
// import { StaticImage } from "gatsby-plugin-image";

// const GtsbyImage = ({ width, height, src, alt, placeholder, layout }) => {
//   return (
//     <StaticImage
//       src={src}
//       alt={alt}
//       placeholder={placeholder}
//       layout={`${layout === undefined ? "constrained" : layout}`}
//       width={width}
//       height={height}
//     />
//   );
// };

// export default GtsbyImage;
